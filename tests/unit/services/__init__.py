"""
Unit tests for the services layer.

Layout mirrors ``services/``:
- ``core/`` — ChatService, RoadmapService
- ``support/`` — SessionManager, IntentDetector, ProfileExtractor, profile_inference
- ``flows/`` — chat_flow, roadmap_flow
- ``test_app_service.py`` — AppService (orchestrator at package root)
"""
